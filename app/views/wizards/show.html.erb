<table>
  <tr>
    <th><%= @wizard.name %></th>
    <th><%= @wizard.dark_or_light() %></th>
  </tr>
  <tr>
    <td><%= content_tag :span, "Level:" %></td>
	<td><%= @wizard.level %></td> 
  </tr>
  <tr>
    <td><%= content_tag :span, "Lives:" %></td>
	<td><%= @wizard.lives %></td> 
  </tr>
    <tr>
    <td><%= content_tag :span, "HP:" %></td>
	<td><%= @wizard.hp %></td> 
  </tr>
  <tr>
    <td><%= content_tag :span, "Attack:" %></td>
	<td><%= @wizard.attack %></td> 
  </tr>
  <tr>
    <td><%= content_tag :span, "Defense:" %></td>
	<td><%= @wizard.defense %></td> 
  </tr>
  <tr>
    <td><%= content_tag :span, "Belongs to User:" %></td>
	<td><%= link_to @wizard.user.email, user_path(@wizard.user) %></td> 
  </tr>
</table>

<%= link_to "increment", wizard_path(@wizard), method: :put, remote: true %>
<%= content_tag :div, id: "battle" do -%>
<%= render :partial => 'battle', :object => @wizards %>
<% end %>