<%= link_to "BATTLE!", wizard_path(@wizard), method: :put, remote: true %>

<%= button_tag "ROCK", class: "move", id: "rock", name: "#{@wizard}" %>
<%= button_tag "PAPER", class: "move", id: "paper", name: "#{@wizard}" %>
<%= button_tag "SCISSORS", class: "move", id: "scissors", name: "#{@wizard}" %>

<%= link_to '<button type="button">paper</button>'.html_safe, wizard_path(@wizard), method: :put, remote: true %>
<%= link_to "Add to cart", {:controller => "car", :action => "add_to_cart", :car => car.id }%>

<%= content_tag :div, id: "battle" do -%>
<%= render :partial => 'battle', :object => @wizards %>
<% end %>

<%= link_to "Run Away!", opponent_session_path(@wizard) %>